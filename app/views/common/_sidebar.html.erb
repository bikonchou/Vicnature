<!-- Sidebar -->
<div class="container">
	<div class="col-md-3">
	    <div class="list-group">
			<h2>商品類別</h2>
			<h3>Product Categories</h3>
	        <hr class="intro-divider">
			<% @category.each do |category| %>
			<%= link_to "#{category.title}", category_products_path(category), class: "list-group-item" %>
			<br>
			<% end %>
		</div>
	</div>
</div>